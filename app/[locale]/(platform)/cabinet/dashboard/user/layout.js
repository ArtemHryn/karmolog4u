import PageProgress from '../../../../../../components/Common/PageProgress/PageProgress';

const UserLayout = ({ children }) => {
  return <PageProgress color="#cfb691">{children}</PageProgress>;
};

export default UserLayout;
